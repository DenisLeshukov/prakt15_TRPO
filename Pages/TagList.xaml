<Page x:Class="prakt15_Leshukov_TRPO.Pages.TagList"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:prakt15_Leshukov_TRPO.Pages"
      xmlns:valid="clr-namespace:prakt15_Leshukov_TRPO.Validators"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="TagList">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="0.5*"/>
        </Grid.ColumnDefinitions>
        <ListView SelectedItem="{Binding selectedTag}" MouseDoubleClick="ListView_MouseDoubleClick" ItemsSource="{Binding tags}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="{Binding Name}"></TextBlock>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <StackPanel x:Name="brand" Grid.Column="1" VerticalAlignment="Center">
            <TextBox >
                <Binding Path="tag.Name" UpdateSourceTrigger="PropertyChanged">
                    <Binding.ValidationRules>
                        <valid:EntryValidation/>
                    </Binding.ValidationRules>
                </Binding>
                
            </TextBox>
            <Button Click="Save">Сохранить</Button>
            <Button Click="Reset">Сбросить</Button>
            <Button Click="Delete">Удалить</Button>
            <Button Click="goBack">Назад</Button>
        </StackPanel>
    </Grid>
</Page>
